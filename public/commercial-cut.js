(()=>{let started=false;const load=()=>{if(started)return;started=true;cleanup();const script=document.createElement('script');script.src='/commercial-cut-runtime.js';script.async=false;script.dataset.commercialCutRuntime='true';document.body.append(script)};const mobile=matchMedia('(max-width:760px)').matches;if(!mobile){load();return}const events=['scroll','wheel','touchstart','pointerdown'];const cleanup=()=>{events.forEach(type=>removeEventListener(type,load));removeEventListener('keydown',load);removeEventListener('hashchange',load);document.removeEventListener('brayro:market-opened',load);document.removeEventListener('rae:opened',load)};events.forEach(type=>addEventListener(type,load,{once:true,passive:true}));addEventListener('keydown',load,{once:true});addEventListener('hashchange',load,{once:true});document.addEventListener('brayro:market-opened',load,{once:true});document.addEventListener('rae:opened',load,{once:true});if(scrollY>0||location.hash)queueMicrotask(load)})();
