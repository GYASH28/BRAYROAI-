const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>[...r.querySelectorAll(s)];const nav=$('[data-page-nav]'),button=$('[data-page-menu]'),menu=$('[data-page-mobile]');const setMenu=open=>{if(!button||!menu)return;button.setAttribute('aria-expanded',String(open));menu.classList.toggle('open',open);menu.setAttribute('aria-hidden',String(!open));document.body.style.overflow=open?'hidden':'';if(open)requestAnimationFrame(()=>menu.querySelector('a')?.focus({preventScroll:true}));else button.focus({preventScroll:true})};button?.addEventListener('click',()=>setMenu(button.getAttribute('aria-expanded')!=='true'));$$('a',menu||document).forEach(a=>a.addEventListener('click',()=>setMenu(false)));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&menu?.classList.contains('open'))setMenu(false)});addEventListener('scroll',()=>nav?.classList.toggle('scrolled',scrollY>35),{passive:true});const plan=new URLSearchParams(location.search).get('plan');if(plan){const target=$(`[data-plan-row="${CSS.escape(plan)}"]`);if(target){target.setAttribute('data-selected','true');setTimeout(()=>target.scrollIntoView({block:'center'}),20)}}