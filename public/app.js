const deferredStyleLinks=['post-fixes-styles','experience-styles'].map(id=>document.getElementById(id)).filter(Boolean);
const deferredStylesReady=Promise.all(deferredStyleLinks.map(link=>{link.media='all';if(link.sheet)return Promise.resolve();return new Promise(resolve=>{link.addEventListener('load',resolve,{once:true});link.addEventListener('error',resolve,{once:true})})}));
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
const coarse=matchMedia('(pointer: coarse)').matches;
const finePointer=matchMedia('(pointer: fine)').matches;
const $=(selector,root=document)=>root.querySelector(selector);
const $$=(selector,root=document)=>[...root.querySelectorAll(selector)];
const clamp=(min,value,max)=>Math.min(max,Math.max(min,value));

class LockedHeroController{
  constructor(){this.hero=$('#top');this.depthRoot=$('[data-depth-root]',this.hero||document);this.depthLayers=this.depthRoot?$$('[data-depth]',this.depthRoot):[];this.heroUI=this.hero?$('.hero-ui',this.hero):null;this.brandType=this.hero?$('.hero-brand-type',this.hero):null;this.px=0;this.py=0;this.visible=true;this.setupLoader();this.ensureSignalDeck();this.setupPointerDepth();if(this.hero&&'IntersectionObserver'in window){this.visibilityObserver=new IntersectionObserver(entries=>{this.visible=Boolean(entries[0]?.isIntersecting);window.__BRAYROAI__?.schedule()},{rootMargin:'25% 0px',threshold:0});this.visibilityObserver.observe(this.hero)}}
  setupLoader(){const loader=$('[data-loader]');if(!loader){document.body.classList.add('hero-ready');return}if(reduced){loader.remove();document.body.classList.add('hero-ready');return}setTimeout(()=>{loader.classList.add('is-done');document.body.classList.add('hero-ready')},920)}
  ensureSignalDeck(){if(!this.hero||$('.hero-signal-deck',this.hero))return;const deck=document.createElement('div');deck.className='hero-signal-deck';deck.setAttribute('aria-hidden','true');deck.innerHTML='<p>BRAYROAI / SIGNAL SYSTEM</p><div>CRAFT <span>AUTHORED</span></div><div>TECH <span>USEFUL</span></div><div>MOTION <span>PURPOSEFUL</span></div>';this.hero.appendChild(deck)}
  setupPointerDepth(){if(!this.hero||!this.depthRoot||reduced||coarse)return;this.depthRoot.addEventListener('pointermove',event=>{const rect=this.depthRoot.getBoundingClientRect();if(!rect.width||!rect.height)return;this.px=(event.clientX-rect.left)/rect.width-.5;this.py=(event.clientY-rect.top)/rect.height-.5;window.__BRAYROAI__?.schedule()},{passive:true});this.depthRoot.addEventListener('pointerleave',()=>{this.px=0;this.py=0;window.__BRAYROAI__?.schedule()})}
  update(){if(!this.hero||!this.depthRoot||reduced||!this.visible)return;const rect=this.hero.getBoundingClientRect();const scrollP=clamp(0,-rect.top/Math.max(1,rect.height),1);const chapterP=clamp(0,(innerHeight-rect.top)/Math.max(1,rect.height+innerHeight),1);this.hero.style.setProperty('--hero-scroll',scrollP.toFixed(4));this.hero.style.setProperty('--chapter-progress',chapterP.toFixed(4));if(this.heroUI){this.heroUI.style.opacity=String(1-clamp(0,scrollP*1.45,1));this.heroUI.style.translate=`0 ${scrollP*-30}px`}if(this.brandType)this.brandType.style.filter=`blur(${scrollP*1.7}px)`;this.depthLayers.forEach(layer=>{const depth=Number(layer.dataset.depth||.3);const x=this.px*34*depth;const y=this.py*24*depth-scrollP*86*depth;const scale=1+scrollP*.035*depth;layer.style.transform=`translate3d(${x}px,${y}px,0) scale(${scale})`})}
}

class NavigationManager{
  constructor(){this.nav=$('[data-nav]');this.progress=$('[data-progress]');this.menuButton=$('[data-menu-button]');this.mobileMenu=$('[data-mobile-menu]');this.navLinks=$$('.desktop-nav a[href^="#"]');this.targets=this.navLinks.map(link=>$(link.getAttribute('href'))).filter(Boolean);this.themeSections=$$('[data-nav-theme]');this.lastY=scrollY;this.bindAnchors();this.bindMenu()}
  bindAnchors(){$$('a[href^="#"]').forEach(link=>link.addEventListener('click',event=>{const id=link.getAttribute('href');if(!id||id==='#')return;const target=$(id);if(!target)return;event.preventDefault();target.scrollIntoView({behavior:reduced?'auto':'smooth',block:'start'});history.replaceState(null,'',id);if(this.mobileMenu?.classList.contains('open'))this.setMenu(false)}))}
  setMenu(open){this.menuButton?.setAttribute('aria-expanded',String(open));this.mobileMenu?.classList.toggle('open',open);this.mobileMenu?.setAttribute('aria-hidden',String(!open));document.body.classList.toggle('menu-open',open);if(open){this.menuWasOpen=true;requestAnimationFrame(()=>this.mobileMenu?.querySelector('a')?.focus({preventScroll:true}))}else if(this.menuWasOpen){this.menuWasOpen=false;this.menuButton?.focus({preventScroll:true})}}
  bindMenu(){if(!this.menuButton||!this.mobileMenu)return;this.mobileMenu.setAttribute('aria-hidden','true');this.menuButton.addEventListener('click',()=>this.setMenu(this.menuButton.getAttribute('aria-expanded')!=='true'));$$('a',this.mobileMenu).forEach(link=>link.addEventListener('click',()=>this.setMenu(false)));document.addEventListener('keydown',event=>{if(event.key==='Escape'&&this.mobileMenu.classList.contains('open'))this.setMenu(false);if(event.key!=='Tab'||this.menuButton.getAttribute('aria-expanded')!=='true')return;const items=$$('a[href],button:not([disabled])',this.mobileMenu).filter(el=>el.offsetParent!==null);if(!items.length)return;const first=items[0],last=items.at(-1);if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus()}});addEventListener('resize',()=>{if(innerWidth>760&&this.menuButton.getAttribute('aria-expanded')==='true')this.setMenu(false)},{passive:true})}
  update(){const y=scrollY,max=Math.max(1,document.documentElement.scrollHeight-innerHeight);if(this.progress)this.progress.style.transform=`scaleX(${clamp(0,y/max,1)})`;this.nav?.classList.toggle('scrolled',y>70);this.nav?.classList.toggle('hidden',y>this.lastY&&y>360);this.lastY=y;const focal=innerHeight*.38;let active=null,best=Infinity;this.targets.forEach(target=>{const rect=target.getBoundingClientRect();const distance=Math.abs(rect.top+Math.min(rect.height,innerHeight)*.38-focal);if(rect.bottom>0&&rect.top<innerHeight&&distance<best){best=distance;active=target}});this.navLinks.forEach(link=>link.classList.toggle('is-active',Boolean(active)&&link.getAttribute('href')===`#${active.id}`));let theme='dark',themeDistance=Infinity;this.themeSections.forEach(section=>{const rect=section.getBoundingClientRect();if(rect.bottom<=0||rect.top>=innerHeight)return;const distance=Math.abs(rect.top-innerHeight*.22);if(distance<themeDistance){themeDistance=distance;theme=section.dataset.navTheme||'dark'}});document.body.classList.toggle('theme-light',theme==='light')}
}

class PlanPriorityController{
  constructor(){
    this.entry=$('[data-plan="makeover"]');this.standard=$('[data-plan="website"]');
    if(this.entry&&this.standard){
      this.standard.removeAttribute('data-plan-recommended');this.standard.classList.remove('plan-card--featured');this.standard.querySelector('.plan-cta')?.classList.remove('plan-cta--featured');
      const badge=this.standard.querySelector('.plan-badge');if(badge){badge.innerHTML='<i></i>START HERE';this.entry.prepend(badge)}
      this.entry.setAttribute('data-plan-recommended','');this.entry.classList.add('plan-card--featured');this.entry.querySelector('.plan-cta')?.classList.add('plan-cta--featured');
      const entryTag=$('.plan-top>span',this.entry),standardTag=$('.plan-top>span',this.standard),entryCta=$('.plan-cta span',this.entry);
      if(entryTag)entryTag.textContent='Lowest entry / fastest upgrade';if(standardTag)standardTag.textContent='Custom build from scratch';if(entryCta)entryCta.textContent='Start at ₹9,999';
      this.entry.setAttribute('aria-label','Digital Makeover, highlighted entry plan, starting at ₹9,999');this.standard.setAttribute('aria-label','Full Website, starting at ₹17,999');
    }
    const care=$$('.care-plans article');if(care.length){care.forEach(item=>item.classList.remove('care-featured'));care[0].classList.add('care-featured');care[0].setAttribute('data-care-highlight','');const label=care[0].querySelector('div');if(label&&!$('.care-badge',label)){const chip=document.createElement('b');chip.className='care-badge';chip.textContent='ENTRY';label.appendChild(chip)}}
    const contactPrimary=$('.contact-primary');if(contactPrimary){const contactText=$('span',contactPrimary);if(contactText)contactText.textContent='Start at ₹9,999';contactPrimary.href='mailto:yashganesh.work@gmail.com?subject=BRAYROAI%20Digital%20Makeover';contactPrimary.setAttribute('aria-label','Start with the ₹9,999 Digital Makeover plan')}
    this.setupRevealRhythm();document.body.classList.add('v10-polished');
  }
  setupRevealRhythm(){for(const selector of ['.plan-grid','.proof-steps','.service-list','.care-plans']){const group=$(selector);if(!group)continue;[...group.children].forEach((item,index)=>item.style.setProperty('--reveal-delay',`${Math.min(index*55,180)}ms`))}}
}

class PointerSystem{
  constructor(){this.orb=$('[data-cursor-orb]');this.context=$('[data-context-cursor]');this.contextLabel=this.context?$('span',this.context):null;this.cx=-300;this.cy=-300;this.tx=-300;this.ty=-300;this.frame=0;if(finePointer&&!reduced)this.bind();else{if(this.orb)this.orb.style.display='none';if(this.context)this.context.style.display='none'}}
  bind(){addEventListener('pointermove',event=>{this.tx=event.clientX;this.ty=event.clientY;if(this.context){this.context.style.left=`${event.clientX}px`;this.context.style.top=`${event.clientY}px`}if(!this.frame)this.frame=requestAnimationFrame(()=>this.drawOrb())},{passive:true});$$('[data-cursor]').forEach(target=>{target.addEventListener('pointerenter',()=>{if(this.contextLabel)this.contextLabel.textContent=target.dataset.cursor||'OPEN';this.context?.classList.add('is-visible')});target.addEventListener('pointerleave',()=>this.context?.classList.remove('is-visible'))});$$('.magnetic').forEach(el=>{el.addEventListener('pointermove',event=>{const rect=el.getBoundingClientRect();el.style.transform=`translate3d(${(event.clientX-rect.left-rect.width/2)*.08}px,${(event.clientY-rect.top-rect.height/2)*.08}px,0)`});el.addEventListener('pointerleave',()=>{el.style.transform=''})});$$('.interactive-surface,.plan-card,.service-row,.care-plans article,.studio-image-v9').forEach(surface=>{surface.addEventListener('pointermove',event=>{const rect=surface.getBoundingClientRect();if(!rect.width||!rect.height)return;const nx=clamp(-1,((event.clientX-rect.left)/rect.width-.5)*2,1),ny=clamp(-1,((event.clientY-rect.top)/rect.height-.5)*2,1);surface.style.setProperty('--pointer-x',nx.toFixed(3));surface.style.setProperty('--pointer-y',ny.toFixed(3));surface.style.setProperty('--spot-x',`${(((event.clientX-rect.left)/rect.width)*100).toFixed(1)}%`);surface.style.setProperty('--spot-y',`${(((event.clientY-rect.top)/rect.height)*100).toFixed(1)}%`)},{passive:true});surface.addEventListener('pointerleave',()=>{surface.style.setProperty('--pointer-x','0');surface.style.setProperty('--pointer-y','0');surface.style.setProperty('--spot-x','50%');surface.style.setProperty('--spot-y','50%')})})}
  drawOrb(){this.cx+=(this.tx-this.cx)*.13;this.cy+=(this.ty-this.cy)*.13;if(this.orb)this.orb.style.transform=`translate3d(${this.cx-110}px,${this.cy-110}px,0)`;if(Math.abs(this.tx-this.cx)>.4||Math.abs(this.ty-this.cy)>.4)this.frame=requestAnimationFrame(()=>this.drawOrb());else this.frame=0}
}

class WorkScene{
  constructor(){this.el=$('[data-work-scene]');this.visible=true;if(this.el&&'IntersectionObserver'in window){this.observer=new IntersectionObserver(entries=>{this.visible=Boolean(entries[0]?.isIntersecting);window.__BRAYROAI__?.schedule()},{rootMargin:'30% 0px',threshold:0});this.observer.observe(this.el)}}
  update(){if(!this.el||!this.visible||reduced||innerWidth<=800)return;const rect=this.el.getBoundingClientRect();const p=clamp(0,(innerHeight*.78-rect.top)/Math.max(innerHeight,rect.height*.72),1);this.el.style.setProperty('--work-progress',p.toFixed(4))}
}

class SystemTabs{
  constructor(){this.panel=$('[data-system-panel]');this.buttons=$$('[data-system-mode]');this.label=$('[data-system-label]');this.copy=$('[data-system-copy]');this.meta=$('[data-system-meta]');this.states={design:{label:'DESIGN',meta:'TYPE / SPACE / MOTION',copy:'Composition, hierarchy, and interaction establish the product character.'},build:{label:'BUILD',meta:'COMPONENTS / STATES / PERFORMANCE',copy:'The visual system becomes responsive components, reliable states, and production behavior.'},ai:{label:'AI',meta:'CONTEXT / TOOLS / AUTOMATION',copy:'Intelligence enters where it improves the task, workflow, or response—not as decoration.'}};this.buttons.forEach(button=>button.addEventListener('click',()=>this.set(button.dataset.systemMode)))}
  set(mode){const state=this.states[mode];if(!state||!this.panel)return;this.panel.dataset.mode=mode;this.buttons.forEach(button=>{const active=button.dataset.systemMode===mode;button.classList.toggle('is-active',active);button.setAttribute('aria-selected',String(active))});if(this.label)this.label.textContent=state.label;if(this.copy)this.copy.textContent=state.copy;if(this.meta)this.meta.textContent=state.meta}
}

class RevealController{
  constructor(){this.items=$$('.reveal-item');if(reduced||!('IntersectionObserver'in window)){this.items.forEach(item=>item.classList.add('is-visible'));return}this.observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');this.observer.unobserve(entry.target)}}),{rootMargin:'0px 0px -7% 0px',threshold:.08});this.items.forEach(item=>this.observer.observe(item))}
}

class BRAYROExperience{
  constructor(){window.__BRAYROAI__=this;this.frame=0;this.hero=new LockedHeroController();this.plans=new PlanPriorityController();this.navigation=new NavigationManager();this.pointer=new PointerSystem();this.work=new WorkScene();this.system=new SystemTabs();this.update=this.update.bind(this);this.schedule=this.schedule.bind(this);addEventListener('scroll',this.schedule,{passive:true});addEventListener('resize',this.schedule,{passive:true});addEventListener('load',this.schedule,{once:true});this.schedule()}
  schedule(){if(!this.frame)this.frame=requestAnimationFrame(this.update)}
  update(){this.frame=0;this.hero.update();this.navigation.update();this.work.update()}
}

const experience=new BRAYROExperience();
deferredStylesReady.finally(()=>{if(!reduced)document.body.classList.add('v9-motion');new RevealController();document.body.classList.add('experience-ready');experience.schedule()});
